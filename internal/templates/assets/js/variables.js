/**
 * Information de session
 */
export const SessionData = {
  isLogged: false,
  username: null,
  notifications: [],
};

/**
 * Modes de saisie activés dans l'éditeur de messages
 */
export const typeMode = {
  BBcode: true,
  MDon: true,
};

export const state = { UserList: [] };

export const pageData = {
  ShowingOnlineMembers: false,
  ConversationWith: "none",
  currentPage: "",
  previousPage: "",
};
